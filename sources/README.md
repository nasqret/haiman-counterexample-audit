# Sources

The article PDF supplied by the user is intentionally excluded from git. Its identity is fixed by `source-manifest.json` and DOI metadata.

To verify a local copy:

```bash
shasum -a 256 Lee_principal_component_not_CM.pdf
```

Expected digest:

```text
78bb80a2ba5194e30df5fb775d9997d479e258efe986199693035babf2d75c6d
```

Do not add the PDF to a public commit without explicit redistribution permission.
